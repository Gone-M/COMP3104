language: node_js
node_js:
  - "18"
dist: focal
script:
  - npm test
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  local_dir: build
  on:
    branch: main
before_install:
  - nvm install 18
